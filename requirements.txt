Django==5.2.6
djangorestframework==3.15.2
django-htmx==1.19.0
crispy-bootstrap5==2024.10
django-crispy-forms==2.3
django-environ==0.11.2
dj-database-url==2.3.0
django-jazzmin==3.0.1
cloudinary==1.41.0
django-cloudinary-storage==0.3.0
gunicorn==22.0.0
whitenoise==6.7.0
psycopg2-binary==2.9.12
reportlab==4.2.5
Pillow==10.4.0
# Renders the attendance QR codes HR prints and sticks on a wall. Pure
# Python; Pillow above turns the matrix into the PNG that gets printed.
qrcode==7.4.2
pytest==8.3.3
pytest-django==4.9.0

# Test-only. esprima parses the JavaScript this system ships, so a syntax
# error cannot reach production on a box with no Node toolchain.
esprima==4.0.1
# Optional: drives a real browser for the offline tests. The browser suite
# skips itself when this or its chromium download is absent.
#   pip install playwright && python -m playwright install chromium
playwright==1.62.0
